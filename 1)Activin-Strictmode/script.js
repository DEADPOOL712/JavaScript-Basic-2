// Strict mode in javaScript : 
'use strict'; // try comment out this ans see what happens 


let hasDriversLicence = false;
let PassTest = true;

if (PassTest) hasDriverLicence = true;
if (hasDriversLicence) console.log(" i can drive ");

const interface = 'Audio'; // As we can see in console error that this word is reseved for future function it will come in future 
